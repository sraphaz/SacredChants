/**
 * Single source of truth for offline cache identity (keep in sync with public/sw.js).
 */
export const OFFLINE_CACHE_NAME = 'sc-offline-v9';
export const OFFLINE_CACHE_NAME_PATTERN = /^sc-offline-v\d+$/;
export const BUNDLE_STORAGE_KEY_PREFIX = 'sc-offline-urls:';
